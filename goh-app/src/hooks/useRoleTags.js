import { firedb } from "../firebase/config";
import { useState } from "react";
import { doc } from "firebase/firestore";

export const useRoleTag = () => {
    const [error, setError] = useState(null)

    const roleTag = async {
        projId,
        ownerid,
        
    }


}